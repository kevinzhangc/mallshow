spring:
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath*:com/mall/show/shopping/dal/persistence/*Mapper.xml
  type-aliases-package: com.mall.show.shopping.dal.entitys
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

